/* import { useDispatch } from "react-redux";
import { RootStateType } from "../../app/store";
import { useAppDispatch } from "../../hooks/reduxHooks";
import { getProfilesByHouseholdId } from "../household/householdSlice";
import { Profile, Avatars } from "./profileTypes";

export const returnAvailableAvatars = (householdId: string) => {
  const dispatch = useAppDispatch();
  const profiles = dispatch(getProfilesByHouseholdId(householdId));

  let availableAvatars;
};
 */
